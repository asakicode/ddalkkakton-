# 로컬: docker compose up -d 후 아래 URL 사용
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ddalkkakton?schema=public"

# Vercel / Neon 등: 대시보드에서 발급한 연결 문자열로 교체
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"
